# pertikaer

## Testing

This repository includes a simple test script to validate the repository structure.

To run the tests:

```bash
./test.sh
```

The test script validates:
- README.md exists and contains content
- Required directories (files, images) exist
- Images directory contains files
